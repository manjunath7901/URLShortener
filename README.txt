#To install virtual environment
pip install virtualenv

#To create virtual environment
virtualenv venv(any_custome name)


#To activate virtual env
venv\Scripts\activate (we have to activate whenevere we are using it).

#to install django
pip install django


#To start the project django inbuilty provides some funcationality 
django-admin startproject URLShortener


to start the server
python manage.py runserver